## What and why

<!-- What this PR changes and why - what problem it solves or what
     capability it adds. -->

## Changes

<!-- Briefly list the key changes. -->

## Checklist

- [ ] `poetry run ruff check .` and `poetry run ruff format --check .` pass
- [ ] `poetry run mypy` passes
- [ ] `poetry run pytest -q --cov=avia_api --cov-report=term-missing --cov-fail-under=99` passes
- [ ] Tests added/updated for the changed behavior
- [ ] `CHANGELOG.md` updated (`[Unreleased]` section) if public behavior changes
- [ ] `README.md` updated if the public API changes
